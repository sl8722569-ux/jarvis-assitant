name: android-apk
on:
  workflow_dispatch:
  push:
    paths:
      - 'android/**'
      - '.github/workflows/android-apk.yml'
permissions:
  contents: write
jobs:
  apk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'
      - name: Build APK
        working-directory: android
        run: gradle assembleRelease --no-daemon
      - name: Name artifact
        run: cp android/app/build/outputs/apk/release/app-release.apk JARVIS-Android.apk
      - uses: actions/upload-artifact@v4
        with:
          name: JARVIS-Android.apk
          path: JARVIS-Android.apk
      - name: Attach to GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="v3.2.0-installers"
          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" JARVIS-Android.apk --clobber
          else
            gh release create "$TAG" JARVIS-Android.apk --title "J.A.R.V.I.S Android APK" --notes "Sideload APK: official WebView of the live PWA (camera/mic allowed). Early Access, debug-signed. Not a Play Store listing."
          fi
